const lisContainer = document.getElementById('list-container')


//write a function
//create 20 divs
//each div must have
//and h1 inside of it with the words "hello world"
// All 20 divs must appear on the dom

function createAllTheDivs(){

    for(let i=0; i<100; i++)
    // 1: create element(s)
    const
}